[project]
name = "doctorassistant-kws-text2token"
version = "0.1.0"
description = "Convert plain text to SherpaONNX KWS keyword tokens for DoctorAssistant."
requires-python = ">=3.10"
dependencies = [
    "pypinyin>=0.54.0",
]

[tool.uv]
package = false
